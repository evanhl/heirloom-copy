<section class="lightbox">
  <div class="scrollable">
    {{#each albums.model}}
      <div class="album" {{bind-attr style=style}} {{action select id}}>
        <span>{{name}}</span>
      </div>
    {{/each}}

    {{partial 'spinner'}}
  </div>
</section>