<div {{bind-attr class=":albums-container"}}>
  <div {{bind-attr class=":nav-filler isSelectionMode::collapsed"}}></div>
  {{#if showToolbar}}
    <div {{bind-attr class=":selection-toolbar  removingToolbar:removing"}}>
      <div class="right">
        {{#toolbar-item iconClass="toolbar-trash-icon" label=(ts 'delete') action="deleteAlbums"}}{{/toolbar-item}}
        <button class="secondary" {{action 'cancel'}}>{{t 'cancel'}}</button>
      </div>
    </div>
  {{/if}}

{{!-- The HTML comments below are to ensure no accidental spaces between inline-block elements --}}
  <div class="create-new-album" {{action 'create'}}>
    <span class="caption">{{t 'albums.create'}}</span>
  </div>{{!--
--}}{{#each album in this}}<!--
   -->{{render 'thumbAlbum' album enlargeDisabled=false}}<!--
-->{{/each}}

  {{partial 'spinner'}}
</div>