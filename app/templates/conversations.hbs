{{#each}}
  <div style="
    padding: 10px;
    background: white;
    width: 300px;
    margin: 10px;"
  >{{name}}</div>
{{/each}}

{{#if loadingMore}}
<div class="spinner"></div>
{{else}}
<div class="spinner-placeholder"></div>
{{/if}}
